"""Generate one or more synthetic GT plume volumes and rendered observations.

TODO:
- set a reproducible seed
- make GT dense volume
- create 1/2/4/8/16 cameras around it
- render observations
- save GT volume + images + camera metadata

Keep data format simple at first: `torch.save` is fine.
"""


def main() -> None:
    raise NotImplementedError


if __name__ == "__main__":
    main()
